package org.example.kotlinBasics.core.syntax.exercises.expressions

/**
 * Task:
 * 1. Write a function `evaluateGrade(score: Int): String` that uses `if` or `when` as an expression
 *    to return:
 *      - "A" if score >= 90
 *      - "B" if score >= 80
 *      - "C" if score >= 70
 *      - "F" otherwise
 *
 * 2. Then in main(), read an integer from the user (or hardcode for now), pass it to evaluateGrade(),
 *    and print the result.
 *
 * 3. (Bonus) Write a function `readIntOrNull()` that tries to parse user input with a `try` expression
 *    and returns null if parse fails, or the integer if success.
 *
 * 4. (Bonus) Use that function to handle invalid inputs gracefully.
 */

fun main() {
    // Write your solution here
}
